CREATE TABLE autores (
    id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
	vigente BOOLEAN NOT NULL DEFAULT true
);

CREATE TABLE editoriales (
    id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
	vigente BOOLEAN NOT NULL DEFAULT true
);

CREATE TABLE generos_literarios (
    id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
	vigente BOOLEAN NOT NULL DEFAULT true
);

CREATE TABLE libros (
    id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    titulo VARCHAR(150) NOT NULL,

    id_autor INT NOT NULL,
    id_editorial INT NOT NULL,
    id_genero_literario INT NOT NULL,
    vigente BOOLEAN NOT NULL DEFAULT true,

    CONSTRAINT fk_libros_autor
        FOREIGN KEY (id_autor)
        REFERENCES autores(id),

    CONSTRAINT fk_libros_editorial
        FOREIGN KEY (id_editorial)
        REFERENCES editoriales(id),

    CONSTRAINT fk_libros_genero
        FOREIGN KEY (id_genero_literario)
        REFERENCES generos_literarios(id)
);

CREATE TABLE stock_libros (
	id INT GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
	
    id_libro INT NOT NULL,
	precio INTEGER NOT NULL,
	disponibles INT NOT NULL,
	fecha_registro DATE NOT NULL,
	vigente BOOLEAN NOT NULL DEFAULT true,

	CONSTRAINT fk_stock_libros_libros
        FOREIGN KEY (id_libro)
        REFERENCES libros(id)
);

CREATE INDEX idx_stock_libros_id_libro ON stock_libros (id_libro);

CREATE TABLE usuarios (
	id INT  GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    email VARCHAR(200) NOT NULL,
    password_hash VARCHAR(250) NOT NULL,
    is_active BOOLEAN NOT NULL DEFAULT true
);

CREATE UNIQUE INDEX idx_usuarios_email ON usuarios (email);

----------

INSERT INTO public.stock_libros (id_libro, precio, disponibles, fecha_registro, vigente) VALUES
(1, 25.50, 15, CURRENT_DATE, true),  -- Cien años de soledad
(2, 22.00, 10, CURRENT_DATE, true),  -- El amor en los tiempos del cólera
(3, 15.00, 8, CURRENT_DATE, true),   -- Crónica de una muerte anunciada
(4, 20.00, 12, CURRENT_DATE, true),  -- La casa de los espíritus
(5, 18.50, 5, CURRENT_DATE, true),   -- Paula
(6, 17.00, 7, CURRENT_DATE, true),   -- Eva Luna
(7, 30.00, 20, CURRENT_DATE, true),  -- El Hobbit
(8, 28.00, 25, CURRENT_DATE, true),  -- La comunidad del anillo
(9, 28.00, 22, CURRENT_DATE, true),  -- Las dos torres
(10, 28.00, 18, CURRENT_DATE, true), -- El retorno del rey
(11, 19.90, 30, CURRENT_DATE, true), -- 1984
(12, 14.50, 15, CURRENT_DATE, true), -- Rebelión en la granja
(13, 35.00, 10, CURRENT_DATE, true), -- It
(14, 24.00, 12, CURRENT_DATE, true), -- El resplandor
(15, 21.00, 9, CURRENT_DATE, true),  -- Cementerio de animales
(16, 26.00, 6, CURRENT_DATE, true),  -- Kafka en la orilla
(17, 23.00, 11, CURRENT_DATE, true), -- Tokio Blues
(18, 20.00, 4, CURRENT_DATE, true),  -- Rayuela
(19, 16.00, 8, CURRENT_DATE, true),  -- Bestiario
(20, 22.50, 40, CURRENT_DATE, true), -- Harry Potter 1
(21, 22.50, 35, CURRENT_DATE, true), -- Harry Potter 2
(22, 22.50, 30, CURRENT_DATE, true), -- Harry Potter 3
(23, 27.00, 14, CURRENT_DATE, true), -- El imperio final
(24, 27.00, 12, CURRENT_DATE, true), -- El pozo de la ascensión
(25, 27.00, 15, CURRENT_DATE, true), -- El héroe de las eras
(26, 12.00, 20, CURRENT_DATE, true), -- Asesinato en el Orient Express
(27, 12.00, 18, CURRENT_DATE, true), -- Diez negritos
(28, 12.00, 15, CURRENT_DATE, true), -- Muerte en el Nilo
(29, 21.50, 10, CURRENT_DATE, true), -- Ensayo sobre la ceguera
(30, 24.00, 5, CURRENT_DATE, true);  -- El otoño del patriarca

